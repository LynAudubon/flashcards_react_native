import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    menu: {
        flex: 1,
        alignItems: "center",
        margin: 10,
        padding: 10,
        justifyContent: "space-around",
        color: "white",
    }
});